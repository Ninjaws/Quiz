export const environment = {
    production: true,
    /** Connecting through the docker compose network is not working for now, so I'll have to use the localhost */
    // apiUrl: 'http://backend:9090',
    apiUrl: 'http://localhost:9090',
  };