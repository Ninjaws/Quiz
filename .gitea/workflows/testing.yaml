name: Quiz App Testing
run-name: ${{ gitea.actor }} is testing Quiz App
on: [push]

jobs:
  Quiz Runner:
    runs-on: ubuntu-latest
    steps:
      - run: docker --version
      - run: docker compose version
      - name: Check out repository code
        uses: actions/checkout@v4
        # Run tests
